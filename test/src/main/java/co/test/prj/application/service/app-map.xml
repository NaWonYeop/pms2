<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.test.prj.application.service.AppMapper">

	<!-- <select id="appSelectList" resultType="co.test.prj.application.service.AppVO">
		SELECT * FROM APPLICATION ORDER BY APP_ID ASC
	</select> -->

	<select id="appSelect" resultType="co.test.prj.application.service.AppVO">
		SELECT * FROM APPLICATION
		WHERE prj_id = #{prj_id} AND master_id = #{master_id} 
	</select>

	<insert id="appInsert"
		parameterType="co.test.prj.application.service.AppVO">
		<selectKey keyProperty="app_id" resultType="int"
			order="BEFORE">
			SELECT NVL(MAX(APP_ID) + 1, 1) AS ID FROM APPLICATION
		</selectKey>
		INSERT INTO APPLICATION (APP_ID, PRJ_ID, MASTER_ID, USER_ID,
		APP_CLSFC, APP_STT)
		VALUES(#{app_id}, #{prj_id}, #{master_id},
		#{user_id},
		#{app_clsfc}, #{app_stt})
	</insert>
	
	<update id="appUpdate" parameterType="co.test.prj.application.service.AppVO">
		UPDATE APPLICATION
		<set>
			<if test="app_clsfc != null">APP_CLSFC = #{app_clsfc},</if>
			<if test="app_stt != null">APP_STT = #{app_stt},</if>
		</set>
		WHERE app_id = #{app_id}
	</update>
	
	<delete id="appDelete" parameterType="co.test.prj.application.service.AppVO">
		DELETE FROM APPLICATION
		WHERE app_id = #{app_id} AND master_id = #{master_id}
	</delete>
</mapper>